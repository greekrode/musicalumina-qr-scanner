VITE_JWT_PUBLIC_KEY=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_URL=
VITE_DATABASE_URL=postgresql://username:password@host:port/database

# Server-side only (no VITE_ prefix). Read by Vite dev proxy and by the
# Vercel serverless function at api/airtable-webhook.ts.
AIRTABLE_WEBHOOK_URL=https://hooks.airtable.com/workflows/v1/genericWebhook/...
